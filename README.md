# kNN
This project implements the K-Nearest Neighbors (KNN) classification algorithm using the Iris dataset. The data is split into training and testing sets, standardized using StandardScaler, and classified with KNN. Model performance is evaluated using accuracy, confusion matrix, and classification report to measure prediction effectiveness.
